name: Multi-Site Deployment

on:
  push:
    branches: [master, main]
  workflow_dispatch:
    inputs:
      site_id:
        description: 'Specific site ID to deploy (leave empty for all)'
        required: false
        type: string
  repository_dispatch:
    types: [site_deployment_request, site_created]

jobs:
  fetch-configs:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Fetch site configurations from Appwrite
        id: set-matrix
        run: |
          # Fetch all site configs from Appwrite database
          CONFIGS=$(curl -X GET \
            "https://fra.cloud.appwrite.io/v1/databases/${{ secrets.APPWRITE_DATABASE_ID }}/collections/deployment_configs/documents" \
            -H "X-Appwrite-Project: ${{ secrets.APPWRITE_PROJECT_ID }}" \
            -H "X-Appwrite-Key: ${{ secrets.APPWRITE_API_KEY }}" \
            -H "Content-Type: application/json")
          
          # Extract documents and format for matrix
          MATRIX=$(echo "$CONFIGS" | jq -c '{include: [.documents[] | {siteId: .siteId, siteName: .siteName, appId: .appId, primaryColor: .primaryColor, secondaryColor: .secondaryColor, domain: .domain}]}')
          
          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT
          echo "Found sites to deploy:"
          echo "$MATRIX" | jq '.'

  build-and-deploy:
    needs: fetch-configs
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{fromJson(needs.fetch-configs.outputs.matrix)}}
      fail-fast: false
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build site - ${{ matrix.siteName }}
        env:
          APP_ID: ${{ matrix.appId }}
          PRIMARY_COLOR: ${{ matrix.primaryColor }}
          SECONDARY_COLOR: ${{ matrix.secondaryColor }}
          SITE_NAME: ${{ matrix.siteName }}
          DOMAIN: ${{ matrix.domain }}
        run: |
          echo "Building ${{ matrix.siteName }} with App ID: ${{ matrix.appId }}"
          npm run build

      - name: Verify Appwrite Hosting Site - ${{ matrix.siteName }}
        id: verify-site
        run: |
          echo "🔍 Verifying site '${{ matrix.siteName }}' exists in Appwrite Hosting..."
          echo "📋 Site ID from database: ${{ matrix.siteId }}"
          
          # Sites are created by DerivForge API, so we just verify it exists
          # Use the siteId from database (which should be the Appwrite Site ID)
          echo "APPWRITE_SITE_ID=${{ matrix.siteId }}" >> $GITHUB_ENV
          
          echo "✅ Using Appwrite Site ID: ${{ matrix.siteId }}"
          echo "🚀 App ID: ${{ matrix.appId }}"
          echo "🎨 Colors: ${{ matrix.primaryColor }} / ${{ matrix.secondaryColor }}"

      - name: Deploy to Appwrite Hosting - ${{ matrix.siteName }}
        if: env.APPWRITE_SITE_ID != ''
        run: |
          echo "🚀 Deploying ${{ matrix.siteName }} to Appwrite Hosting..."
          echo "📋 Site ID: ${{ env.APPWRITE_SITE_ID }}"
          
          # Install Appwrite CLI for deployment
          npm install -g appwrite-cli@latest
          
          # Login to Appwrite
          appwrite client --endpoint https://fra.cloud.appwrite.io/v1 --project "${{ secrets.APPWRITE_PROJECT_ID }}" --key "${{ secrets.APPWRITE_API_KEY }}"
          
          # Deploy to Appwrite Hosting using CLI
          # Note: Appwrite CLI deploy hosting command format
          appwrite deploy hosting \
            --siteId "${{ env.APPWRITE_SITE_ID }}" \
            --project "${{ secrets.APPWRITE_PROJECT_ID }}" \
            --key "${{ secrets.APPWRITE_API_KEY }}" \
            --dist "dist" \
            --entrypoint "index.html" || {
              echo "⚠️ CLI deployment failed, trying direct API..."
              
              # Fallback: Try direct API deployment
              DEPLOY_RESPONSE=$(curl -s -X POST \
                "https://fra.cloud.appwrite.io/v1/sites/${{ env.APPWRITE_SITE_ID }}/deployments" \
                -H "X-Appwrite-Project: ${{ secrets.APPWRITE_PROJECT_ID }}" \
                -H "X-Appwrite-Key: ${{ secrets.APPWRITE_API_KEY }}" \
                -H "Content-Type: application/json" \
                -F "entrypoint=index.html" \
                -F "code=@dist" || echo '{"error":"failed"}')
              
              echo "API Response: $DEPLOY_RESPONSE"
            }
          
          echo "✅ Deployment process completed for ${{ matrix.siteName }}"
          
          # Update status in database
          curl -s -X PUT \
            "https://fra.cloud.appwrite.io/v1/databases/${{ secrets.APPWRITE_DATABASE_ID }}/collections/deployment_configs/documents/${{ matrix.siteId }}" \
            -H "X-Appwrite-Project: ${{ secrets.APPWRITE_PROJECT_ID }}" \
            -H "X-Appwrite-Key: ${{ secrets.APPWRITE_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{\"status\": \"deployed\", \"updatedAt\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"}" || echo "⚠️ Could not update status"

      - name: Upload build artifact
        uses: actions/upload-artifact@v3
        with:
          name: site-${{ matrix.siteId }}
          path: dist/
          retention-days: 7

  notify-completion:
    needs: build-and-deploy
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Deployment Summary
        run: |
          echo "🎉 Multi-site deployment completed!"
          echo "Check the artifacts for each site build"
